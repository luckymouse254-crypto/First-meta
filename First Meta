from flask import Flask
app = Flask(_name_)

@app.route('/')
def home():
    return "First Meta is Live 🚀 Builder: Andrea Kisumu V0.2.4"

# Vercel needs this
def handler(request):
    return app(request)
